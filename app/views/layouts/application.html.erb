<!DOCTYPE html>
<html>
  <head>
    <title>PoikatsuHiroba</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm">
        <div class="container-fluid">
        <a class="navbar-brand btn btn-outline-primary py-1 px-3" href="<%= root_path %>">ポイ活広場</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav mx-auto">
              <li class="nav-item"><%= link_to "About", about_path, class: "nav-link" %></li>
              <% if user_signed_in? %>
                <li class="nav-item"><%= link_to "おすすめなポイ活サイトを投稿(新規投稿)", new_post_path, class: "nav-link" %></li>
                <li class="nav-item"><%= link_to "ポイ活サイト一覧", posts_path, class: "nav-link" %></li>
                <li class="nav-item"><%= link_to "マイページ", mypage_users_path, class: "nav-link" %></li>
                <li class="nav-item"><%= link_to "ユーザーを探す", search_users_path, class: "nav-link" %></li>
              <% else %>
                <li class="nav-item"><%= link_to "管理者ログイン", new_admin_session_path, class: "nav-link" %></li>
              <% end %>
            </ul>
            <ul class="navbar-nav">
              <% if user_signed_in? %>
                <li class="nav-item"><%= link_to "Logout", destroy_user_session_path, method: :delete, class: "btn btn-outline-danger ms-2" %></li>
              <% else %>
                <li class="nav-item"><%= link_to "Sign Up", new_user_registration_path, class: "btn btn-outline-primary ms-2" %></li>
                <li class="nav-item"><%= link_to "Sign In", new_user_session_path, class: "btn btn-primary ms-2" %></li>
                <li class="nav-item"><%= link_to "ゲストの方はこちら", guest_login_path, method: :post, class: "btn btn-primary ms-2" %></li>
              <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>

    <% flash.each do |key, message| %>
      <p class="<%= key %>"><%= message %></p>
    <% end %>

    <%= yield %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
